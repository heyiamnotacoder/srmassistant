# protocol/

Drop your review protocol documents here before running `/srma-extract`:

- PROSPERO application (PDF, Word, or text)
- PICO criteria / protocol notes
- An existing data extraction sheet template (CSV or XLSX) — if provided, its columns define the extraction schema fields

Everything in this folder is read during schema generation.
